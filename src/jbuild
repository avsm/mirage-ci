(jbuild_version 1)

(library
 ((name        mirage_ci)
  (public_name mirage-ci)
  (wrapped     false)
  (preprocess  (per_file ((pps (ppx_sexp_conv)) (datakit_toml opam_bulk_build))))
  (libraries (dockerfile.opam datakit-ci toml fpath asetmap bos cmdliner rresult sexplib tls ptime))
 ))
