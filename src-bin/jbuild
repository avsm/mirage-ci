(executables 
 ((names (opamBulkCI opamRepoCI opamImageCI))
  (libraries (mirage-ci dockerfile))
  (flags (:standard -short-paths)) 
 ))

(install
 ((section bin)
  (files (
   (opamBulkCI.exe as opam-bulk-ci)
   (opamRepoCI.exe as opam-repo-ci)
;;   (opamImageCI.exe as opam-image-ci)
  ))))
